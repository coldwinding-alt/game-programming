# Halloween 主题一段式提示词

---

## 1) Logo（替换文件：`Assets/rimrush/Resources/rimrush/Images/logo.png`，最终尺寸 `305x96`）

```text
为2D篮球游戏制作一个高端万圣节主题LOGO，透明背景PNG，必须只输出一个logo主体，不要任何背景场景，文字内容必须是 MOON LANTERN PARK，风格要求为高级奇幻篮球招牌风、粗壮字形、强识别度、小尺寸仍清晰可读、橙金与深紫为主并加入少量月光青蓝补色、轻微金属高光、边缘干净利落、无锯齿感、无脏边，允许加入月牙球场弧线、悬挂灯笼、篮板或篮筐等克制的图形元素，但整体要热闹神秘、不阴森不恐怖，构图要求logo居中且四周留安全边距不裁切，技术要求sRGB、高清、干净、无噪点、无水印、无签名、无额外文字，负向约束为模糊、低清晰度、细字难读、字体破碎、背景场景、白底、黑底、水印、签名、多余文字、血腥、恐怖脸、噪点、过度锐化光晕，生成参数为优先尺寸1220x384再缩到305x96，输出PNG透明。
```

---

## 2) 菜单背景 A（替换帧：`interface.png` 的 `bg10000`，最终尺寸 `1398x480`）

```text
制作一张超宽2D游戏主菜单背景图，主题为高端万圣节篮球赛，场景为室内球馆夜场，紫蓝主光与橙色点缀灯光，电影级体积光和轻雾，整体高级且干净，必须保留画面中央45%区域为低细节低对比的UI安全区用于放置菜单按钮，视觉重点放在左右两侧，不要出现人物特写、文字、logo、记分牌文字，风格统一为premium Halloween basketball、polished high-end 2D game art、cinematic lighting、rich but controlled contrast、readable midtones、no gore，技术要求sRGB、高清、低噪点、边缘干净，负向约束为文字、logo、水印、签名、中央区域杂乱、过暗看不清、过曝高光、血腥、恐怖贴脸、透视畸变、模糊、噪点严重，生成参数为优先尺寸2796x960再缩到1398x480，输出PNG。
```

---

## 3) 菜单背景 B（替换帧：`interface.png` 的 `bg1red0000`，最终尺寸 `1398x480`）

```text
制作一张超宽2D游戏菜单背景，万圣节篮球主题且整体高级，主色调偏暖为橙色与暗红并用少量紫色辅助，营造节日夜赛氛围，场景包含球馆空间层次、灯光束、轻微烟雾和适量装饰元素，必须保留中央45%宽度为UI可读安全区且低细节低对比，两侧可更丰富，禁止出现任何文字、logo、水印和人物前景，风格要求polished high-end 2D game visual、cinematic but clean、no gore、readable for game UI overlay，技术要求sRGB、高清、细节清楚但不脏乱，负向约束为文字、logo、水印、签名、中央高对比杂物、血腥、惊悚脸、强噪点、低清晰度、过度锐化、模糊，生成参数为优先尺寸2796x960再缩到1398x480，输出PNG。
```

---

## 4) 菜单背景 C（替换帧：`interface.png` 的 `bg2blue0000`，最终尺寸 `1398x480`）

```text
制作一张超宽2D菜单背景，万圣节篮球主题月夜版本，主色调为青蓝与靛紫并以橙色作小面积点缀，整体高级干净有电影感，场景为精致室内球馆且空间深度明显，中央45%区域必须保留为按钮安全区并保持低细节低干扰低高光，避免中央亮斑，禁止任何文字、logo、人物前景、记分牌文字，风格统一为premium Halloween basketball game art、controlled contrast、readable midtones、no gore，技术要求sRGB、高清、低噪点、可直接用于游戏UI底图，负向约束为文字、logo、水印、签名、中央杂乱、中央强亮点、模糊、低分辨率、噪点、血腥元素、恐怖贴脸，生成参数为优先尺寸2796x960再缩到1398x480，输出PNG。
```

---

## 5) 比赛球馆背景（替换帧：`gameplay.png` 的 `0bg_gameplay0000`，最终尺寸 `1398x480`）

```text
制作一张用于横版2D篮球比赛的球馆背景图，万圣节主题且具有高级质感，必须采用侧视构图以适配横版体育游戏，包含观众席、灯光氛围和木地板球场，但不要画入前景篮筐、前景篮板和巨大UI元素，要求比赛可读性高并保证下方中部球员活动区域不过于花哨，地板与背景层次清晰但不抢主体，禁止文字、logo和水印，风格为premium Halloween basketball、polished high-end 2D art、cinematic lighting、readable gameplay background、no gore，技术要求sRGB、高清、干净、低噪点，负向约束为前景篮筐、前景篮板、文字、logo、水印、签名、过暗看不清、过曝、模糊、噪点、血腥恐怖，生成参数为优先尺寸2796x960再缩到1398x480，输出PNG。
```

---

## 6) 篮筐主体（替换帧：`gameplay.png` 的 `BasketGraphic0000`，最终尺寸 `107x140`，透明）

```text
制作一个横版2D篮球游戏用的篮筐主体sprite，透明背景PNG，内容包含篮板、篮圈和篮网并采用侧视角，结构清晰完整，风格为高端万圣节篮球主题即白色干净篮板、橙色发光金属篮圈、白色篮网带轻微紫色阴影，轮廓清楚且细节简洁并适合小尺寸显示，必须居中构图不裁切，不要背景不要文字不要水印，技术要求sRGB且边缘干净无白边无黑边并可直接进图集，负向约束为背景场景、文字、水印、签名、模糊边缘、脏边、断裂网格、结构不完整、透视错误、噪点，生成参数为优先尺寸428x560再缩到107x140，输出PNG透明。
```

---

## 7) 篮筐前缘覆盖（替换帧：`gameplay.png` 的 `FrontEar0000`，最终尺寸 `64x13`，透明）

```text
制作一个横版2D篮球游戏篮筐前缘覆盖sprite，透明背景PNG，形状为细长弧形金属环片，风格与万圣节篮筐主体一致即橙红色发光金属质感、线条干净、边缘清晰并适合非常小尺寸显示，必须水平居中且四周保留透明像素并且不能裁切，不要背景不要文字不要水印，技术要求sRGB、清晰、低噪点、透明通道干净，负向约束为背景、文字、水印、签名、模糊、锯齿严重、噪点、粗糙笔触、形状歪斜，生成参数为优先尺寸256x52再缩到64x13，输出PNG透明。
```

---

## 8) 护盾技能起手与持续动画（新图集：`skillfx.png` 的 `ShieldMC0000` 与 `ShieldMC20000..ShieldMC20020`，关键源尺寸 `110x144` 与 `135x176`，透明）

```text
为横版2D篮球游戏制作一套高端万圣节主题护盾技能特效，透明背景PNG，仅输出单个技能视觉件，不要角色不要篮筐不要篮球不要背景场景，视觉方向为“篮筐前的幽冥结界”，整体材质是黑曜紫半透明膜面、月白能量高光、少量烛火金边缘和轻烟残影，要求形体清楚、中心读得清、边缘高级克制、不廉价不卡通、不使用旧版青蓝电感火焰描边，画面应适合后续拆成起手下落帧与持续衰减帧，构图上主体必须居中并保留完整上缘和底缘，技术要求为sRGB、高清、4x以上超采样后缩回、透明通道干净、无白边无黑边无噪点无水印无文字，负向约束为背景场景、人物、篮筐、篮球、文字、logo、水印、签名、廉价火焰、卡通表情、血腥、恐怖脸、过曝辉光、边缘脏污、模糊失焦，优先输出 1024x1536 或更高分辨率透明PNG，后续手工裁切为 `ShieldMC0000` 与 `ShieldMC20000..ShieldMC20020` 使用。
```

---

## 9) 传送门技能关键视觉件（新图集：`skillfx.png` 的 `teleport10000`、`teleport20000`、`teleport30000..30003`、`teleport40000`，透明）

```text
为横版2D篮球游戏制作一套高级万圣节传送门技能特效关键视觉件，透明背景PNG，只输出传送门本体和爆发能量，不要人物不要场景不要文字，风格参考成熟女巫系法术门但更电影化克制，主色为幽紫与月白，点缀少量烛火金，要求同时兼容黑环扩张、中心能量核、爆发四阶段和横向白闪，环体必须薄而清晰、中心要有深邃感、爆发要有空间撕裂与灵异能量感，不要旧式青蓝电圈，不要廉价科技UI感，不要夸张漫画闪电，技术要求sRGB、高清、4x以上超采样、透明通道干净、边缘锐利、无白边无黑边无噪点无水印无签名，负向约束为背景、人物、文字、logo、水印、卡通化、机甲科技风、过度复杂花纹、血腥、恐怖脸、脏雾、模糊、低清晰度，优先输出 1250x1250 或更高分辨率透明PNG，后续手工拆分为黑环 `teleport10000`、中心核 `teleport20000`、爆发序列 `teleport30000..30003` 与白闪 `teleport40000`。
```

---

## 10) 超级扣篮拖尾组件（替换 `texture2.png` 内 `man_fire_01` 依赖的 `fx_Blur_mol0..4` 与 `fx_fire_0..4`）

```text
为横版2D篮球游戏超级扣篮技能制作一组 DragonBones 贴图组件，透明背景或纯深色临时底均可但最终要能干净抠出，内容只包括南瓜余烬式火焰片、熔火拖尾、小型模糊能量条和热浪碎片，不要角色不要篮球不要篮筐不要文字，整体风格要高级、克制、动势强，强调飞扑与砸筐瞬间的推进感和热量，主色为熔橙、烛金、焦紫与少量月白高光，不允许出现廉价卡通火焰、科技感蓝火、血腥元素、表情化造型，组件边缘必须清晰、形体适合小矩形裁切、同一组组件之间风格统一，技术要求sRGB、高清、4x以上超采样、低噪点、无水印无签名、禁止文字，负向约束为角色轮廓、背景场景、篮筐、篮球、文字、logo、水印、白边黑边、低清晰度、过曝、脏污、卡通感、蓝色电流，优先输出一张 1536x1024 或更高分辨率的多组件 sheet，后续手工回填 `fx_Blur_mol0..4` 与 `fx_fire_0..4`。
```

---

## 11) 超级冲刺烟迹组件（替换 `texture2.png` 内 `shield_animation_01 / shield_anim` 依赖的 `gsgbfyjgkh`、`fx_smoke_0..7`、`part1`、`fx_spl_0`、`fx_spl2_0`）

```text
为横版2D篮球游戏超级冲刺技能制作一组 DragonBones 贴图组件，透明背景或纯深色临时底均可但最终需要可干净抠图，只输出高速烟迹、裂光、神秘粒子、长条速度残影和少量火星，不要角色不要场景不要文字，风格定位为高级万圣节速度魔法而不是科技推进器，主色为冷紫烟雾、灰紫月雾、少量烛火橙和月白裂光，轮廓要利落、方向性明确、适合横向拉伸与分层播放，不要旧式烟圈感，不要廉价漫画冲刺线，不要机甲喷射感，技术要求sRGB、高清、4x以上超采样、低噪点、无水印无签名无文字、边缘干净可直接回填到现有矩形框，负向约束为人物、背景、文字、logo、水印、蓝色科技喷焰、卡通表情、血腥、恐怖脸、脏边、模糊失焦、严重噪点，优先输出一张 1536x1024 或更高分辨率的多组件 sheet，后续手工回填 `gsgbfyjgkh`、`fx_smoke_0..7`、`part1`、`fx_spl_0`、`fx_spl2_0`。
```

---

## 12) DragonBones 组件总提示词组（用于一次性出超级扣篮与超级冲刺组件 sheet）

```text
为横版2D篮球游戏制作一张高端万圣节技能特效组件总表，深灰或纯黑临时底便于抠图，画面中均匀排布多个独立组件且彼此不接触，组件类型必须覆盖：5个熔火拖尾、5个小型模糊余烬、8个高速烟迹或横向烟丝、1个超宽速度残影、1个细长竖向裂光、1个小型裂光、若干微粒火星，整体风格统一为 premium Halloween sports magic FX，主色为焦橙、烛金、冷紫、灰紫和少量月白，材质高级、边缘清晰、适合小尺寸裁切与动画复用，不要人物不要篮筐不要篮球不要文字不要logo，不要科技HUD感，不要卡通夸张火焰，不要血腥恐怖元素，技术要求sRGB、高清、4x以上超采样、低噪点、无水印无签名、每个组件四周留出安全空白、禁止互相遮挡，优先输出 1536x1024 或更高分辨率PNG。
```

